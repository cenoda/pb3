# 3D PC Builder

**Project Charter & Technical Direction**  
Version 1.0 · 2026-08-08

---

## 1. 프로젝트 목적

이 프로젝트는 PC 부품 선택과 조립을 **설명 가능한 성능 예측**과 **실제 치수 기반의 3D 물리 검증**으로 연결하는 것을 목표로 한다.

핵심은 두 가지다.

1. **성능 예측**  
   게임·작업 부하에 대해 단일 숫자가 아닌 **예상 범위 + 신뢰도 + 데이터 버전**을 제공한다.  
   병목, VRAM/RAM 한계, 전력 제한의 영향을 설명한다.

2. **3D 물리 검증**  
   단순한 스펙 호환을 넘어서, 실제 치수와 형상을 기준으로 장착 가능 여부와 간섭을 시각적으로 판정한다.

상업적으로 기존 서비스를 꺾거나 시장을 독점하는 것이 1차 목표가 아니다.  
이 프로젝트는 기술적으로 깊게 파고, 오픈소스 기반으로 지속 가능한 결과물을 남기는 것을 우선한다.  
반응이 좋으면 계속 발전시키고, 그렇지 않더라도 완성도 높은 엔진과 데이터 구조를 남기는 것을 성공으로 본다.

---

## 2. 핵심 철학

- **모르는 것은 추정하지 않는다.**  
  데이터가 부족하면 가짜 숫자를 만들어내지 않고 “현재 신뢰할 만한 추정 불가”라고 명시한다.

- **깊이를 우선하고 범위를 제한한다.**  
  지원하는 부품·게임·케이스 수는 작게 시작하되, 지원하는 범위 안에서는 설명 가능하고 일관된 결과를 낸다.

- **관심사를 명확히 분리한다.**
  - 논리 호환성 ≠ 물리 충돌
  - 기준 성능 ≠ 환경에 따른 보정
  - 시각 모델 ≠ 충돌/장착 데이터

- **데이터 중심으로 설계한다.**  
  새로운 부품을 추가할 때 코드 수정이 최소화되도록, 부품은 `part.json` + `model.glb`로 완전하게 표현한다.

- **오픈소스는 무료 노동력 확보 수단이 아니다.**  
  데이터 형식, 장착 규격, 엔진을 공개하여 다른 사람이 확장·포크·자체 호스팅할 수 있게 하는 것이 목적이다.  
  고품질 공식 모델은 소수 핵심 기여자가 책임지는 구조를 인정한다.

---

## 3. 최종 목표 상태 (C)

장기적으로 달성하려는 상태:

- 사용자가 부품을 선택하면
  - 논리 호환성 검사
  - 설명 가능한 성능 예상 (범위 + 병목 + 신뢰도)
  - 실제 치수 기반 3D 자동 장착
  - 물리 간섭 및 여유 공간 판정
  - 냉각·전력 조건과 성능 보정의 연동

이 모든 것이 하나의 일관된 데이터 모델 위에서 동작한다.

---

## 4. 개발 순서

순서를 엄격히 지킨다.

### 0단계 — 수직 슬라이스 (기술 검증)

목적: 전체 구조가 실제로 연결되는지 확인하고, 초반 모멘텀을 확보한다.  
**제품 MVP가 아니다.**

**포함 범위 (고정)**
- 단일 견적 화면
- 케이스 1개
- 메인보드 1개
- CPU 2개
- GPU 2개
- 쿨러 1개
- 게임 1개 / 그래픽 프리셋 1개 / 해상도 3개
- 매우 단순한 예상 FPS 범위
- CPU·GPU 변경 시 성능 결과와 3D 모델이 즉시 갱신
- 부품은 JSON + GLB로 추가 가능
- 현재 견적 상태 보존 (URL 또는 로컬)

**명시적으로 금지**
- 추가 케이스/부품
- RGB 편집, 조립 애니메이션
- 정밀 충돌 검사
- 냉각·공기 흐름 연동
- 로그인, 커뮤니티, 실시간 가격
- 관리자 도구, 모델 제작 자동화
- UI 폴리싱

**종료 조건**  
다음 시나리오가 처음부터 끝까지 작동하면 즉시 종료한다.

1. 사용자가 CPU와 GPU를 선택한다.
2. 견적 상태가 변경된다.
3. 3D 화면에서 GPU가 교체된다.
4. 선택한 게임의 해상도별 예상 성능 범위가 갱신된다.
5. 페이지를 새로고침해도 같은 구성을 복원할 수 있다.

종료 후 `vertical-slice-v0` 태그를 남기고, 성능 엔진 1단계가 끝날 때까지 3D 관련 기능 추가를 동결한다.  
(데이터 계약 문제나 슬라이스 자체가 깨지는 버그만 수정 가능)

### 1단계 — 성능 예측 엔진 (주력)

기준 성능 모델과 환경 보정 모델을 분리하여 설계한다.

**기준 성능 모델**
- 통제된 기준 조건(적절한 냉각, 제조사 기본 설정)에서의 성능
- 입력: CPU/GPU, 게임·워크로드, 해상도, 그래픽 설정, 업스케일링/프레임생성, RAM/VRAM, 기본 전력 제한
- 출력: 예상 범위 + 근거 + 신뢰도 + 데이터 버전

**환경 보정 모델**
- 1단계에서는 실제 온도·유체 시뮬레이션을 하지 않는다.
- 허용하는 보정 변수는 제한적이다.
  - CPU/GPU 전력 제한
  - 냉각 상태 (충분 / 경계 / 부족) — 사용자가 선택하거나 실측 근거가 있는 경우만
  - 순간 부하 vs 장시간 부하
- 근거가 없는 경우 보정하지 않고 “냉각 조건에 따른 지속 성능 변화는 계산하지 않음”이라고 명시한다.

나중에 3D 냉각 시스템이 완성되면, 정규화된 값(냉각 여유, 흡기 제한 등)을 이 보정 계층의 입력으로 넣는다.  
성능 엔진 자체를 다시 뜯지 않아도 되도록 인터페이스를 먼저 설계한다.

### 2단계 — 기본 견적 서비스

일반적인 부품 선택, 필터, 논리 호환성, 가격 합산, 저장·공유 기능을 연결한다.  
이 단계는 기술적으로 가장 불확실한 부분이 아니며, 기존 견적 사이트 수준을 최소 기준으로 한다.

### 3단계 — 3D 조립 및 물리 검증

지원 케이스와 부품을 점진적으로 늘리면서 다음을 깊게 구현한다.

- anchor / socket 기반 자동 장착
- collision / clearance 판정
- 장착 위치·방향 변경
- 냉각 조건과 성능 보정 연동

---

## 5. 성능 데이터 원칙

### 1차 출처
- **게임**: 우리가 정의한 표준 러너로 수집한 원시 프레임 데이터 (PresentMon 계열 권장)
- **작업 부하**: 자동화·재현 가능한 공개 벤치마크 프로필 우선 (Phoronix Test Suite / OpenBenchmarking 등)

각 결과에는 숫자만이 아니라 다음 조건을 함께 기록한다.

- 하드웨어 구성
- 게임/패치 버전, 드라이버 버전
- 해상도, 그래픽 프리셋 및 주요 옵션
- 업스케일링·프레임 생성 여부
- 전력 제한, 평균 클럭·온도
- 평균 FPS, 1% low, 프레임타임 분포
- 측정 도구와 버전

### 외부 리뷰 자료
교차 검증과 상대 성능 사전값으로만 사용한다.  
재현 가능한 조건이 부족한 자료는 신뢰도 높은 실측으로 취급하지 않는다.

### 정규화 원칙
조건이 다른 결과를 무리하게 하나의 숫자로 합치지 않는다.  
충분히 가까운 조건끼리만 같은 기준 집합으로 취급하며, 그렇지 않으면 별도 프로필로 보관하거나 “데이터 부족”으로 남긴다.

---

## 6. 부품 데이터 모델 원칙

모든 부품은 다음 두 파일로 표현한다.

```
parts/
└── {category}/
    └── {id}/
        ├── part.json
        └── model.glb
```

- `part.json`: 정체성, 스펙, 인터페이스, 장착 정보, 기하 정보, 성능 점수, 상업 정보, 출처와 신뢰도
- `model.glb`: 시각 메시 + 충돌체 + 장착점 + 여유 공간 노드를 하나의 파일에 포함

필수 노드 규칙:
- `visual:*`
- `collision:*`
- `anchor:*`
- `socket:*`
- `clearance:*`

좌표계와 단위는 전 부품 통일 (mm, Y-up).

모델 등급:
- **Verified**: 치수와 장착점까지 검증된 공식 모델
- **Community**: 자동 검사는 통과했으나 실물 검증 없음
- **Experimental**: 생성형 또는 참고용

AI 모델링은 시각용 초안 제작 비용을 줄이는 용도로만 사용한다.  
Verified 등급의 정확도(장착면, 전원 커넥터 위치 등)는 수동 검증을 전제로 한다.

---

## 7. 성공 기준

이 프로젝트의 성공은 다음으로 정의한다.

- 제한된 범위 안에서라도 **설명 가능한 성능 예측**과 **치수 기반 3D 검증**이 실제로 동작하는 결과물을 만든다.
- 데이터 구조와 엔진이 오픈소스로 공개되어 다른 사람이 확장할 수 있다.
- “이런 방식으로도 PC 견적과 조립을 볼 수 있구나”라는 기술적 인상을 남긴다.

시장 점유율이나 빠른 수익화는 성공 기준에 포함하지 않는다.

---

## 8. 현재 상태

- 철학, 개발 순서, 성능 엔진 경계, 0단계 범위와 종료 조건이 확정됨
- 0단계 홈: `docs/phases/phase-0/`
- 0단계 범위 문서: `docs/phases/phase-0/specs/phase-0.md`
- 0단계 데이터 계약 (`vs0`): `docs/phases/phase-0/specs/vertical-slice-data-contract.md`
- 0단계 fixture 배치 완료 (`parts/**`, `benchmarks/vs0/**`)
- 0단계 구현 및 태그 `vertical-slice-v0` 완료
- 1단계 `perf1` 성능 엔진 구현·검증·closeout 완료 (현재 fixture 값은 전부 stub)
- 다음 작업: Phase 2 M0 범위·계약·`implementation_plan.md` 작성; Phase 2 구현은 미착수
